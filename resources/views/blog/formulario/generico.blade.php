<label> @yield('formTitle') </label>

<form>
    @yield('formulario-especifico')

    <button type="submit" class="btn btn-primary"> @yield('txtBtn1') </button>
    <button type="submit" class="btn btn-primary"> @yield('txtBtn2') </button>
</form>